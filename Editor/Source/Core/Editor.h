#pragma once

#include <Cosmos.h>

namespace Cosmos
{
	class Editor : public Application
	{
	public:

		/// @brief constructor
		Editor(const ApplicationCreateInfo& ci);

		/// @brief destructor
		virtual ~Editor();

	private:

	};
}